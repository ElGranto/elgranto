$( document ).ready(function() {
  
  var slideWidth = $('.slides article').width();
  // $('.slides article').css({'height':slideWidth+'px'});
  // $('.slides article a').css({'height':slideWidth+'px'});
  // $('.slides article').css({'background-size':slideWidth+'px'});
  
});